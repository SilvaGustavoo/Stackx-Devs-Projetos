Quem nunca tentou encontrar o caminho nos jogos de labirinto? Dependendo da complexidade do desenho, isso pode tomar um tempo considerável, a não ser que houvesse um roteiro a ser seguido.

enquanto nao chegar ao fim do labirinto faça
    esquerda <- (angulo + 90°)
    direita <- (angulo - 90°)
    caminho <- E0
    angulo <- 90°
    ande um bloco a frente
    escreva(vire a esquerda)
    esquerda
    enquanto o valor do caminho for diferente de A1 faça
        ande um bloco a frente
    fim_enquanto

    escreva(vire a direita)
    direita
    enquanto o valor do caminho for diferente de A4 faça
        ande um bloco a frente
    fim_enquanto

    escreva(vire a direita)
    direita
    ande um bloco a frente
    escreva(vire a esquerda)
    esquerda
    enquanto o valor do caminho for diferente de B6 faça
        ande um bloco a frente
    fim_enquanto

    escreva(vire a direita)
    direita
    enquanto o valor do caminho for diferente de F6 faça
        ande um bloco a frente
    fim_enquanto

    escreva(vire a esquerda)
    esquerda
    enquanto o valor do caminho for diferente de F8 faça
        ande um bloco a frente
    fim_enquanto

    escreva(vire a direita)
    direita
    enquanto o valor do caminho for diferente de I8 faça
        ande um bloco a frente
    fim_enquanto

    escreva(vire a esquerda)
    esquerda
    ande um bloco a frente
    escreva(vire a direita)
    direita
    ande um bloco a frente
    escreva(vire a direita)
    direita
    enquanto o valor do caminho for diferente de J7 faça
        ande um bloco a frente
    fim_enquanto

    escreva(vire a direita)
    direita
    enquanto o valor do caminho for diferente de G7 faça
        ande um bloco a frente
    fim_enquanto

    escreva(vire a esquerda)
    esquerda
    enquanto o valor do caminho for diferente de G4 faça
        ande um bloco a frente
    fim_enquanto

    escreva(vire a direita)
    direita
    enquanto o valor do caminho for diferente de E4 faça
        ande um bloco a frente
    fim_enquanto

    escreva(vire a direita)
    direita
    ande um bloco a frente
    escreva(vire a esquerda)
    esquerda
    enquanto o valor do caminho for diferente de C5 faça
        ande um bloco a frente
    fim_enquanto

    escreva(vire a esquerda)
    esquerda
    enquanto o valor do caminho for diferente de C2 faça
        ande um bloco a frente
    fim_enquanto

    escreva(vire a esquerda)
    esquerda
    enquanto o valor do caminho for diferente de E2 faça
        ande um bloco a frente
    fim_enquanto

    escreva(vire a esquerda)
    esquerda
    ande um bloco a frente
    escreva(vire a direita)
    direita
    enquanto o valor do caminho for diferente de G3 faça
        ande um bloco a frente
    fim_enquanto

    escreva(vire a direita)
    direita
    enquanto o valor do caminho for diferente de G1 faça
        ande um bloco a frente
    fim_enquanto

    escreva(vire a esquerda)
    esquerda
    enquanto o valor do caminho for diferente de J1 faça
        ande um bloco a frente
    fim_enquanto

    escreva(vire a esquerda)
    esquerda
    ande um bloco a frente
    escreva(vire a esquerda)
    esquerda
    ande um bloco a frente
    escreva(vire a direita)
    direita
    ande um bloco a frente
    escreva(vire a direita)
    direita
    ande um bloco a frente
    escreva(vire a esquerda)
    esquerda
    ande um bloco a frente
    escreva(vire a esquerda)
    esquerda
    ande um bloco a frente
    escreva(vire a direita)
    direita
    enquanto o valor do caminho for diferente de I6 faça
        ande um bloco a frente
    fim_enquanto

    escreva(vire a direita)
    direita
    ande um bloco a frente
    escreva(vire a direita)
    direita
    ande um bloco a frente
    escreva(vire a esquerda)
    esquerda
    ande um bloco a frente
fim_enquanto